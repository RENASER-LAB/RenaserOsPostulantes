node_modules/
dist/
dist-ssr/
*.local

.env
.env.*.local

.vite/
*.tsbuildinfo

.DS_Store
Thumbs.db

.idea/
.vscode/*
!.vscode/extensions.json

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.env.local

# Las capturas de revision son de usar y tirar
capturas/

# El artefacto publicado del maquetado: 2,3 MB generados. La fuente son los
# `maquetado/*.body.html`, que sí van al repositorio.
portal-candidato-ex.html

# Registro de sesión de impeccable, no es del proyecto
.impeccable/questions/

# Estado de sesion del modo live de impeccable, y los tableros de calidad que
# se descargan para mirar. Ninguno es del proyecto.
.impeccable/live/
.impeccable/worlds/

# Las skills de las herramientas de IA. A diferencia del backend, aqui no se
# versionan: nadie del equipo las necesita para levantar el portal.
.agents/
.claude/skills/
skills-lock.json

# La llave de firma y su contrasena. NUNCA al repositorio: con ellas
# cualquiera puede firmar un paquete que Android acepta como si fuera esta app.
# La llave vive fuera del proyecto, en ~/llaves/.
android/keystore.properties

# Salida de Playwright: capturas y trazas de cada corrida, no versionable.
test-results/
playwright-report/
.claude/launch.json
